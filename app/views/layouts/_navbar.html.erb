<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">MyPost</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Home', posts_path, class: 'nav-link active' %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Categorias
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% Category.all.each do |cat| %>
              <li><%= link_to cat.name, category_path(cat), class: 'dropdown-item' %></li>
            <% end %>
            <% if logged_in? && current_user.admin? %>
              <li class="dropdown-divider"></li>
              <li class = 'dropdown-item'><%= link_to new_category_path do %>
                <i class="fas fa-plus"></i>
                Nueva categoria
                <% end %>
              </li>
            <% end %>
          </ul>
        </li>
        <% if logged_in? %>
          <li class="nav-item dropdown">

            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.username %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <li><%= link_to 'Editar', edit_user_path(current_user), class: 'dropdown-item' %></li>
              <li><%= link_to 'Perfil', user_path(current_user), class: 'dropdown-item' %></li>
              <li><%= link_to 'Cerrar sesion', logout_path, class: 'dropdown-item' %></li>
            </ul>
          </li>
          <li class="nav-item">
            <%= link_to 'Nuevo post', new_post_path, class: 'btn btn-primary nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Registrarse', register_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Login', login_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>